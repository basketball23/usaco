#include <iostream>

using namespace std;

int main() {
    int n, k;
    cin >> n >> k;

    // Use AND and XOR operators to find sum of elements
    // & bitwise AND
    // | bitwise OR
    // ^ or xor bitwise XOR
}